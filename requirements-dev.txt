pytest>=8.0
ruff>=0.6
pip-audit>=2.7
# S4 测试矩阵切 PG：嵌入式 PostgreSQL（本机零安装，自动下载 PG 16 二进制）。
# 设置了 ITSM_TEST_DATABASE_URI 指向外部 PG 时无需本依赖。
# PyPI 当前稳定系列为 0.1.x；此前的 >=0.9 不存在，导致全新测试环境无法安装。
pgserver>=0.1.4,<0.2
