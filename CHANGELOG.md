# 变更日志

本文档记录 ITSM 运维管理系统的所有重要变更。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

---

## [未发布] — 2026-08-30

### 历史巡检审核计时回填（2026-09-10）

- 增加数据迁移：按真实首次提交版本的 UTC 时间转换北京本地实施结束时间，重算工作量；最新记录和版本均已退回的旧执行中任务同步为退回修改。审核中及完成任务的错误计时同步修正，不发送重复通知。
- 无提交记录、审核状态冲突、新草稿、实施开始晚于首次提交及已有更早结束时间的任务保留原值，避免猜测历史。巡检详情不再将缺少结束时间的非执行任务显示为进行中。

### 设备导入模板移除证书日期（2026-09-10）

- 新下载的设备导入模板移除「证书到期日期」，同步移除示例值和填写说明，避免后续列错位。保留存量设备数据与旧模板该字段的导入兼容。

### 巡检实施计时边界（2026-09-09）

- 实施结束时间与实际工作量在首次提交审核时冻结，待审核、退回修改、重传及审核通过不再追加实施时间。通知在提交/退回/通过时均展示实际实施结束时间。
- 历史缺少结束时间的审核任务不再按当前时间继续累计；后续审核流转时仅使用可核验的原始提交时间补齐，不以审核完成时间代替实施结束时间。

### 巡检审核退回状态与通知（2026-09-09）

- 巡检审核退回时任务进入「退回修改」，修订后重新提交进入「待审核」，保留实施开始时间；看板、筛选、统计、导出和上传入口支持该状态。
- 渠道通知明确显示「待审核 → 退回修改」、退回原因和修改要求；仅勾选需修改检查项时也带上生成的要求，站内通知不再只保留原因或要求之一。

### 设备导入客户下拉与手填（2026-09-08）

- 修复范围观测模式下客户列表可见、导入允许但模板客户下拉为空的问题：模板客户候选与设备导入预检使用同一实际生效范围；启用范围强制时仍仅返回授权客户。无候选时清空虚构示例客户并明确提示，不再显示有下拉的误导说明。

- 设备导入模板按下载者可见范围提供现有客户完整名称下拉，同时允许手动填写列表外名称；单元格提示与填写说明同步说明精确匹配规则。下载后新建的客户可手填名称导入，不存在或不可见客户仍由预检拒绝，不自动创建客户。

### SPA 页面安全头修复（2026-09-08）

- 修复文件下载安全头误应用于 SPA HTML/JS/CSS 导致白屏：应用入口及非上传静态资源保留站点 CSP，不设置文档 sandbox；外网已登录访问前端 assets 不再误触发文件操作 MFA。报告等业务文件仍保留 sandbox、MFA 和权限检查，上传静态地址仍禁止绕过。

### 外网工作台与模板下载（2026-09-08）

- 外网登录后可读取与内网一致的工作台概览，统计和快捷入口正常展示；仅精确放行概览只读地址，业务模块沿用合并后的外网许可及账号权限。
- 内外网已登录账号均可按原有模块权限下载导入模板，下载无需额外 MFA 验证。下载改为获取 Excel 后保存，失败提示原因，不再打开登录页。工作台加载失败显示错误原因，避免误显示为空数据。

### 外网与文件安全复查（2026-09-08）

- 敏感上传文件禁止通过静态地址直接访问；工单进展图片保留权限校验的兼容地址。外网报告/配置/密码读取使用账号 MFA 操作验证；拓扑资源及工单图片允许有效 MFA 登录会话，运维和资产模块沿用远端现有外网许可，模板下载仍免额外 MFA。外网敏感读取不能由全局操作码开关关闭。
- 仅信任 `ITSM_TRUSTED_PROXY_NETWORKS` 指定代理的客户端 IP 转发头（默认回环地址）；未受信代理的转发请求按外网处理，可信网段配置读取异常关闭敏感接口。生产必须核验代理地址与上传目录直出配置。
- 报告、配置、资产表增加文件内容、Office 宏/嵌入对象/外部资源、ZIP 路径/压缩比/展开大小、图片像素、XML 实体及 Excel 公式/规模检查；配置上传两条入口补齐统一校验；报告采用独立随机目录防同名覆盖，保留原始可读文件名。
- 旧二进制 `.doc/.xls/.vsd` 新上传需转换为 `.docx/.xlsx/.vsdx` 或 PDF；资产表公式需粘贴为值，加密包及嵌套压缩包需解包后重新整理。结构检查不等同杀毒，生产扫描/CDR 与存量文件检查仍待完成。
- 提交版本列表不再下发配置正文，预览按受控接口加载；文件预览明确显示访问拒绝原因，PDF 预览增加沙箱限制。复查与待验收清单见 `docs/external-file-security-review-2026-09-08.md`。
- 报告管理及巡检记录的报告下载、预览、历史版本和资料包仅在外网强制账号 MFA；前端统一携带短时操作令牌，旧报告下载地址同样受保护，报告静态原文件禁止外网绕过。内网继续沿用现有操作码设置。

- 外网开放运维管理全部菜单和接口（工单、故障、任务安排、巡检人员/记录、任务/设备检查模板、报告及运维导入导出/删除），仍由原账号权限和数据范围控制；开放审核清单只读依赖。配置正文不再随版本列表下发，配置读取与含配置的资料包需账号 MFA，资料下载支持操作令牌。

- 修复外网访问守卫把 `/app/assets/export-*.js` 当作批量导出重定向到登录页，导致动态模块加载失败：公共构建资源 GET/HEAD 正常放行，业务导出和配置文件限制保持生效。

- 修复在线更新脚本的发布通道探测：配置代理时仍会独立测试直连，探测日志保留 curl 返回码和 HTTP 状态码，区分网络错误、Release 资产不存在及镜像不支持 Range，避免统一误报“所有通道不可达”。

- 外网开放工单/故障、设备列表、机柜、拓扑及必要字典和字段元数据；首页落到工单，仍按账号权限和客户范围访问。客户管理、系统管理、批量导出及删除限制不变，HTTPS 策略不变。
- 外网查看设备密码和配置预览/下载/对比强制验证已绑定的账号 MFA，不受全局操作码开关关闭影响；配置读取补审计及禁止缓存，静态配置文件外网不可绕过验证直出，下载改为携带操作令牌的同源请求。

- 已停用设备名称统一显示灰色并提示“已停用”，覆盖设备列表、机柜设备表及机柜图；停用灰色优先于授权到期颜色，在用设备与无状态的手动机柜条目保持原样。

- 网络安全版本控制表及对应导出日期列统一为“授权开始 → 授权截止 → 建设时间”，移除该预设的证书到期日期列；其它预设与已存设备数据保持不变。

- 修复设备名称到期提示覆盖默认链接样式的问题：正常／无授权日期设备恢复原主题主色及 500 字重，仅到期异常覆盖颜色；详情链接使用 Router 内部路径，避免出现 /app/app/devices。

- 网络安全版本控制表及对应导出预设仅包含网络／安全设备类型，排除会议等其它设备；移除预设中的接口列，保留其它视图与设备接口数据。切回资产／密码视图解除版本表范围限制。
- 设备名称按授权状态变色：已过期红色，当天及未来 30 天内到期橙色；桌面与移动卡片共用显示，名称仍可打开详情。

- 设备表授权截止保留原日期，增加已过期红色提示、今日及 30 天内到期橙色提示，三种表格视图和移动端统一使用。
- 设备管理新增全部／网络设备／安全设备快捷类别，基于现有设备类型聚合，支持与客户、机房等条件叠加，列表、客户树、普通导出和密码审核导出共用筛选；切换类别清空原单一类型条件。

- 工作台到期授权按距离今天的天数由短到长排序后取前 8 条，避免多年过期项优先占满；每项固定为客户名称、设备名称与到期提示两行，长名称省略且保留悬停全文。

- 设备新增/编辑表单的品牌改为可搜索、可清空和自定义填写的下拉，复用系统品牌字典；品牌与型号保持等宽，避免下拉挤压型号输入框。

### 报告中心归并

- 自动生成关闭时，旧“补生成报告”接口同样拒绝调用，仅支持人工上传。
- “全部”列表把已关联报告附件合并到巡检/故障/工单主记录，保留业务审核状态，避免同一记录与附件重复计数；文件名搜索可定位所属记录。
- 隐藏 reports 根目录中符合旧巡检生成器命名规则的自动报告，不按文件大小判断、不删除原文件；人工上传目录不受此规则影响。巡检报告优先显示人工上传件，自动生成仍默认关闭。

### 客户下拉与机柜层级

- 设备模板字典下拉改为命名范围引用，显式显示下拉箭头，客户选项沿用当前账号可见客户的完整名称。
- 机柜树改为客户 → 机房 → 机柜；同名机房按客户隔离，未填写机房单独归组，详情同步显示机房。机柜设备明细按起始 U 位倒序，在分页前排序，与机柜图从顶部到底部一致。

### 账号安全界面精简

- 修改密码弹窗收窄，统一顶部标签与字段间距；提交按钮移至底部与关闭对齐。嵌入的身份验证器移除嵌套大卡片，标题、状态和换绑操作紧凑排列，说明不再继承卡片标题的粗体。
- 身份验证器管理合并进“修改密码”弹窗，删除重复状态卡片和大幅成功占位；绑定状态、用途说明及换绑按钮紧凑展示。旧 MFA 管理链接兼容跳转至同一弹窗，首次登录强制绑定流程及账号 MFA 验证规则不变。

### 设备导入模板填写规范

- 导入时自定义设备类型、品牌按名称自动补入设置字典（最多64字符），与设备同事务提交；预检、跳过及失败不留字典记录，同名不重复新增。导入成功同步刷新页面类型/品牌下拉。
- 接口填写说明补充完整网络设备与会议设备示例，统一数量、接口类型、输入/输出方向及顿号分隔；保留自由填写，不按设备类型猜测或强制套用接口配置。
- U位提示、填写说明和示例行统一为：设备占用23-24U，起始U位填23、占用U数填2；明确占用U数不是结束U号，仅占1U时填1。
- 设备模板保持原字段名称、顺序及第1行表头，第2行示例用浅黄色区分；选中填写单元格显示格式提示，日期统一显示 `yyyy-mm-dd` 并校验有效日期，端口/U位/功率/设备ID提示整数规则，序列号与版本等列预设文本格式。保留系统字典下拉，自定义机柜、机房、品牌、类型不被Excel下拉强行拦截；填写说明按模板顺序列出每个字段的规则与示例，明确粘贴数据仍需服务器预检。

### 设备导入后列表刷新

- 修复设备导入“确认执行”收到 HTTP 200 但整批校验失败时仍提示成功的问题：后端仅在设备与批次记录同事务提交后确认 `committed`，前端必须收到提交确认才显示成功；预检数量标为预计值，拒绝执行保留错误明细和弹窗，并写拒绝审计，成功审计附批次号。旧批次的幂等重试仍可正确确认。
- 修复在客户设备表格中确认 Excel 导入后只刷新隐藏客户树、当前表格仍显示导入前数据的问题；设备资产表、设备密码表和网络安全版本控制表均重新请求当前列表，保持当前筛选、列预设不变。新增前端“预检→确认执行→日期读回”回归和后端建设/授权/证书日期新增、更新读回回归。

### 通知、权限、拓扑资源与报告文件归并

- 修复巡检记录“补传资料”把 SPA 基础路径重复拼入路由而落入 `ROUTE_NOT_FOUND`；设备 Excel 模板统一使用“授权开始/授权截止”并兼容旧表头，设备管理和巡检资产表两条导入链路统一保留建设、授权与证书日期类型；规则库版本等文本单元格即使被 Excel 识别为日期，也只保存 `YYYY-MM-DD`，不再附加无意义的 `00:00:00`。
- 核心设备文本配置允许不关联设备、直接填写配置名称；选择文件时自动使用原文件名，提交版本和设备配置备份列表均显示可辨识名称，多个配置不再全部显示为笼统的“配置”。
- 账号登录与查看设备密码等高风险操作统一复用同一个账号 MFA：移除第二套“操作码”绑定入口，保留高风险操作短时令牌及失败锁定；迁移会在账号 MFA 缺失时接管原操作 MFA，并清空废弃的第二套密钥字段。
- 配置文本、巡检报告、任务现场报告及知识库附件统一使用自适应在线预览窗口：默认按视口放大，支持拖动右下角调整大小和一键全屏，预览内容随窗口伸缩，不再受固定高度限制。
- 任务安排卡片的状态圆点扩大并增加内外双层高对比描边，强化已安排、执行中、待审核和已完成等状态的快速识别。
- 设备登录方式统一增加“本地串口”，同步进入设备编辑下拉、后端字段校验、设备字典与 Excel 导入模板数据验证。
- 巡检提交版本的审核结果改为自适应紧凑网格，审核头部汇总合格、需修改和不适用数量；历史记录缺少审核人时不再显示“审核人：-”，仅展示审核时间。
- 当前用户 API 补充登录 MFA 绑定状态；所有已登录用户均可从右上角账号菜单进入身份验证器管理，未绑定账号在系统顶部持续显示“立即绑定”安全提醒，不再依赖管理员用户管理页；首次登录和全局强制场景继续阻断至绑定完成。
- 巡检企业微信通知收敛为手机端短格式：任务标题独占首行，派发/状态/待审核动作置于正文首行，仅保留地点、工程师、任务期限及当前阶段的实施时间；状态通知不再混入合同时效，巡检通知统一移除无法直接访问的详情链接。
- 初始密码账号首次登录时即使全局 MFA 开关关闭，也必须先绑定登录 MFA，再进入首次改密；绑定页增加明确提示并在强制绑定期间只展示登录 MFA。
- 部门负责人职责与审核权限联动：被设置为任一部门负责人后自动获得巡检、工单、派单和合同例外审核权限，用户级显式 deny 仍可收回；提交部门未设负责人时沿组织树向上寻找最近一级负责人。
- 巡检审核改为提交时明确选择审核人：候选人按实际 `inspection:review` 权限生成并优先当前部门负责人，任务资料提交、记录提交和重提均保存本轮审核人；只有选定审核人或管理员可执行审核，站内及企业微信待审通知只投递给选定人员。部门负责人数据范围同时覆盖其负责部门及全部下级，不再因负责人本人挂在上级部门而看不到运维科提交记录。
- 巡检配置 ZIP/全套资料上传使用独立 30 分钟客户端超时并展示进度；取消前端写死的 95MB 上限，改为读取服务端实际配置（默认请求总量 1024MB、配置 ZIP 900MB，可分别通过 `ITSM_MAX_UPLOAD_MB` / `ITSM_INSPECTION_CONFIG_MAX_MB` 调整），服务端 413 与超时/连接错误均返回明确提示。
- 设备列表进入具体客户后，机房位置候选按该客户实时过滤；切换客户会清空旧机房条件，不再混入其他客户机房名称。
- 设备 Excel 导入保留原生日期单元格类型，并兼容 `YYYY-MM-DD`、`YYYY/M/D`、`YYYY.M.D`；无效日期不再静默丢弃而是在预检中逐行报错。设备资产表补显“接口”，网络安全版本表补显“接口、证书到期日期”，保证导入字段有对应的快捷表格视图。
- 巡检资料的设备候选覆盖任务客户的启用及停用设备，并在下拉中同时显示“设备名称、设备类型、IP、停用状态”；服务端校验所选设备必须属于任务客户。巡检记录审核后新增“补传资料”入口，复用任务资料上传流程。
- 审核通过后自动生成巡检 Word 报告默认关闭，正式报告暂不作为资料完整性门禁；后续模板完善后可通过 `ITSM_AUTO_GENERATE_INSPECTION_REPORT=1` 显式开启。
- 巡检任务新增独立的“状态变更”通知事件：单条编辑、批量变更、任务看板及旧兼容入口均在事务提交后推送；任务资料提交待审核、审核通过和审核退回同步接入企业微信群通知，同值保存不重复推送。
- 企业微信群工单通知改为结构化详情，明确展示实际被派工程师、客户/联系人、故障时间与现象、故障地点、前往时间及处置进展；站内相对路径统一转换为可点击的完整“查看详情”链接，不再单独输出 `/app/...` 裸路径。巡检派发通知同步展示巡检地点、前往时间和工程师。
- 工单新增报修联系人、联系电话、故障地点和前往时间，任务安排新增前往时间；字段进入统一元数据、详情与编辑流程，并通过迁移补齐数据库列。
- 通知规则改为一次事务“保存全部规则”，接收角色使用角色中文名称；角色权限矩阵改为勾选后统一保存，避免逐格请求和半保存状态，两个批量写入口均保留审计日志。
- 普通查看账号与管理员使用同一套拓扑标准模板和规范图标库；模板入口按查看权限开放，资源 URL 增加内容指纹，消除不同账号或浏览器命中旧图标缓存的问题。
- 报告中心按巡检/工单/故障业务记录归并报告文件：系统生成报告与人工上传报告同属一行、分别下载；文件行不再错误跳转 `/app/`，从根源上消除“同一巡检显示两条报告记录”的误解。

### 工单/故障处置时效与敏感凭据传输信封

- 任务安排 Excel 导出日期改按“任务期限”区间交集筛选，提供本周/本月快捷范围并支持多选任务状态；不再把导出日期误用于合同时效开始日期。
- 任务看板卡片的负责人、合同时效和任务期限摘要与任务标题左边缘对齐；进入批量模式显示复选框时同步调整缩进。
- 设备密码表的列表快捷视图、列设置预设和审核导出统一为凭据操作顺序：机房位置、设备身份、登录凭据、在用/改密审计、机柜位置、序列号。
- 设备密码表页面将 IP 与端口合并为单列 `IP:端口` 展示；底层模型、编辑、导入模板、API 和审核导出继续保留两个独立字段。
- 企业微信通知渠道由自建应用逐用户推送统一为企业微信群机器人 Webhook；Webhook 通过凭据传输信封提交并加密存储，只允许企业微信官方 HTTPS 地址，同一事件每个群只发送一次，渠道测试无需填写 userid。
- 新增统一北京工作时间引擎（工作日 08:30–12:00、13:30–17:30，8 小时折算 1 人天），工单状态机写入结构化计时事件并按轮次冻结快照；挂起区间从处置时效扣除，审核退回、客户退回、关闭和重开保留明确计时语义。
- 任务日期选择器接入国务院办公厅 2026 年法定节假日/调休单一日历，直观区分工作日、周末、节假日和调休上班日并汇总任务期限；实施耗时同步排除法定节假日、计入调休工作日，未发布年份明确回退普通周历。任务看板同状态内改按阶段开始时间排序：待执行看合同时效开始、已安排看任务期限开始、实施阶段看实际开始。
- 工单和故障列表、详情、列配置与 Excel 导出统一增加报障/处置起止、响应时长、处置时效、实际人天、闭环时长及计时来源；关联故障只读复用工单计时，手工故障增加处置开始时间并标记历史估算值。
- 新增默认 dry-run 的 `scripts/backfill_ticket_timing.py`，对存量明确时间生成事件/估算快照，输出 SHA256 报告；`--apply` 强制记录配对备份标识且整体事务提交。
- 新增与登录 MFA Session 绑定的一次性 P-256 ECDH、HKDF-SHA256、AES-256-GCM 双向凭据传输信封；AAD 绑定用户、用户名、`auth_version`、登录/MFA 时间、Session 摘要、purpose、HTTP 方法/路径和目标对象，challenge 在数据库行锁下单次消费，临时私钥使用独立包装密钥保护。
- 设备密码新增/修改/当前与历史查看、设备 Excel 整文件导入、设备密码导出包口令、备份导入/导出口令、AI Key 和通知渠道 secret 已接入信封；required 模式拒绝 raw 敏感字段，optional 模式仅记录兼容调用元数据，不记录秘密。
- 前端敏感查看值仅保留在局部弹窗并在 60 秒、隐藏、关闭或卸载时清理；不具备安全上下文/Web Crypto 时直接阻断敏感操作，不再静默回退明文。数据库 Fernet 静态加密保持独立不变。
- 新增信封/下载票据迁移、过期私钥清空与 7 天元数据清理、包装密钥及 purpose 启动校验、协议篡改/重放/跨目标/加密导入导出和存量计时回填测试。

### 客户层级、设备批量治理、机柜容量与拓扑标准模板

- 客户列表改为基于 `parent_id` 的任意层级业务树，地区仅作为筛选和任务安排属性；新增上级单位编辑、路径导入导出、跨地区层级、循环/父客户删除保护及只读数据预演脚本。
- 设备新增“额定功率”主字段和单/双/四电源字典，统一列表、详情、编辑、批量修改、三类导出、Excel 模板、巡检资产清单与机柜摘要；已上架设备的机房位置只从机柜派生。
- 设备 Excel 模板增加当前客户/类型/品牌/网络/电源等下拉及填写说明；导入升级为预检、网络类型显式映射、仅新增/仅更新/upsert、空值语义、错误明细、U 位冲突校验和幂等批次；同客户同名设备允许存在，覆盖导入遇到歧义时必须使用设备 ID。
- 修复设备导入全量跳过仍可“确认执行”的误导：预检返回逐行跳过原因，无新增/更新动作时禁止提交；“仅更新”找不到正式设备时明确说明机柜手工记录不等于设备资产，并引导改用“仅新增”或“新增并更新”。
- 修复新用户在可信内网 HTTP 登录后首次改密被 HTTPS 守卫阻断：本人自助改密与设备凭据操作共用可信网段例外；外网 HTTP、管理员重置他人密码和密码导出仍强制 HTTPS。
- 设备页增加机房/非机房筛选、筛选结果或已选设备导出、批量删除影响预览和事务删除；普通导出与密码审核导出复用相同筛选器和数据范围。
- 设备列表增加“设备资产表 / 设备密码表 / 网络安全版本控制表”三种常驻快速视图；密码表默认掩码，具备明文查看权限时可按行显示/隐藏，并沿用传输信封、审计及 60 秒自动清理。
- 网络安全版本控制表收敛为版本管理视图：设备名称后紧接系统版本和规则库版本，移除机柜、U 位、供电与额定功率列；快捷视图、列设置和 Excel 导出共用同一预设顺序。
- 机柜摘要固定为“起始U位、名称、品牌、型号、IP、额定功率”，关联设备功率只读设备主数据；新增功率完整率、PDU 剩余容量/负载率、BTU/h 参考和容量完整性告警。
- 设备字典、客户类别、角色、地区、故障分类和部门支持一次事务拖动排序，树形设置仅允许同级排序，移动端保留上移/下移操作。
- 新建网络/会议拓扑会复制带标题栏、常用图形区和真实标准线段图例的版本化模板；旧图仅通过显式操作插入图例，保存增加 XML 安全校验，拓扑与机柜接口补齐客户数据范围。
- 机柜详情统计压缩为两行摘要，容量说明改为悬浮提示，并缩小 42U 机柜图，避免 1080p 页面被统计区和机柜图占满；拓扑模板与标准图标库改按查看权限加载，编辑用户无需新增权限也能使用规范资源。
- 发布脚本在迁移前执行设备同名组和机柜功率冲突只读检查并保存 JSON 报告；新增额定功率、电源字典、拓扑模板版本和导入批次迁移；首次生产预检确认同客户多台同名设备属于真实资产后撤销错误唯一约束。

---

## [v2.5] — 2026-08-12

### Vue 单轨、字段统一与身份安全

- 建立设备、客户、巡检、工单、故障、备件六类实体字段注册中心，列表、详情、表单和导出共享字段名称、顺序、格式与敏感属性；Vue 表格和导出弹窗运行时读取统一元数据。
- 删除 SSR 表单认证与界面切换能力，系统固定 Vue 单轨；`/login`、`/logout` 仅保留 SPA 兼容跳转壳，测试登录基座统一迁移到 `/api/auth/login`。
- 新增默认关闭的腾讯身份验证器 TOTP、恢复码、操作短令牌、登录锁定、会话吊销/闲置超时/IP 绑定和离职清理。
- 补齐真实 IP 表审计、设备改密与遗留 reveal 审计、敏感接口 HTTPS 守卫/禁缓存/限流/日志脱敏；前端明文和剪贴板 10 秒清理。
- 密钥轮换覆盖导出密码、通知渠道 JSON 和用户 MFA 种子；新增 PBKDF2/AES-256-GCM 可选主密钥锁定与 CLI 解锁，补充安全部署、离职和轮换 SOP。

---

## [v2.4] — 2026-08-07

### 报告中心重构：客户分桶 → 列表式（巡检/故障/工单/报告文件统一聚合）

- **列表式展示**：`/app/reports` 由「客户折叠面板」改为与巡检记录/故障记录一致的
  DataTable 列表（类型 tag / 标题（巡检·工单可跳详情）/ 客户 / 日期 / 状态 / 报告文件 / 操作），
  分页 + 标题搜索，移动端自动卡片化；页头保留「覆盖客户 / 记录总数」统计（随 tab 与筛选联动）
- **修复「有报告却显示无报告」**：文件反查索引原先只认正式 Word 报告
  （`reports/` 目录 + `report_file` 列），现扩展覆盖工程师上传的现场报告
  （`submitted_report` + `submission_versions.report_file` + `static/uploads/inspection_reports|ticket_reports` 扫描），
  记录行携带报告文件名与下载链接，报告文件行归属客户
- **`/api/reports` 重写**：返回分页统一列表 `{items, total, stats}`（DataTable 契约）；
  巡检行正式报告优先、现场报告兜底；工单/故障行解析处理报告；
  新增安全下载端点 `GET /api/reports/file/<path>`（realpath 防路径穿越）；
  客户/日期筛选统一作用于四类行（原文件桶不受客户过滤的不一致已修正）；
  删除报告仅限 `reports/` 根目录文件（复用 `POST /reports/delete/<name>`，`deletable` 标记）
- **测试**：TestReports 重写（列表契约/分页/筛选/搜索/现场报告归属/正式报告行/下载防穿越），
  新增 conftest `report_dirs` 夹具隔离磁盘目录；修复 `report_seed` 夹具 yield 在
  app_context 内导致请求复用过期 identity map 的隐患；全量 591 用例通过 + ruff 干净

---

### 修复：update.sh git pull 卡死 + 全链路多通道

- **根因**：`[3/6] git pull` 无超时无兜底——服务器直连 GitHub 被墙时无限挂起（此前多通道只覆盖 vue-dist 下载）
- **修复**：公共多通道区（代理/直连/镜像列表 + 探测函数）供 git pull 与 vue-dist 共用；
  `git pull` 限时 60s（不再卡死）→ 失败清理 index.lock → 代理 → 8 个镜像逐个兜底；
  `[5.6]` 复用 [3/6] 探测结果（ITSM_AVAILABLE），不再重复探测
- `ITSM_PROXIES`（逗号分隔多个）/ `ITSM_PROXY` / 系统 https_proxy 自动识别；`ITSM_MIRRORS` 可追加镜像；`ITSM_SKIP_MIRRORS=1` 跳过镜像

---

## [v2.3.1] — 2026-08-05

### 修复：服务器增量升级漏列导致的巡检记录 500

- **根因**：已发布的迁移 `9a8b7c6d5e4f`/`5f4e3d2c1b0a` 在后续提交中被修改（追加加列逻辑），
  服务器 alembic_version 早已越过这两个版本 → 修改后的加列永不执行 → PG 上
  `submission_versions` 缺 `revision_requirements`/`review_checklist_json` 列 →
  `/api/inspections/<id>/versions` 500（本地全新库测试无法覆盖该场景）
- **修复**：新迁移 `6f5e4d3c2b1a` 幂等补齐缺失列（revision_requirements、
  review_checklist_json，并兜底检查 inspections.submitted_report、
  inspection_task_templates.required_assets_json），PG/SQLite 通用
- **测试**：新增 `tests/test_migration_incremental_fix.py`——模拟服务器真实场景
  （alembic 已标记 head 但列缺失 → 升级补列 → versions API 正常），防再犯
- **约定**：后续新列/新表必须新建迁移文件，严禁修改已发布的迁移 revision

---

## [v2.3] — 2026-08-05

### 巡检审核：报告在线预览 + 检查项清单勾选（V23）

- **报告在线预览**：巡检审核弹窗双栏布局——左栏在线预览当前待审核版本报告（Word→docx-preview 渲染、PDF→内嵌、图片→直接显示、文本配置→纯文本、Excel/zip→下载提示，新增 `docx-preview` 依赖），右栏边看边审
- **审核检查项清单（系统级可配置）**：`SystemSetting` 键 `inspection_review_checklist` 默认 9 项（核心设备配置备份/拓扑图/资产信息/链路状态及信息/路由信息/现场图片/设备除尘/机房环境/会议测试），系统管理新增「巡检审核清单」页（admin 增删改/排序/启停）
- **三态勾选留痕**：每项 合格/需修改/不适用（`review_checklist_json` 随版本存储）；版本时间线展示每轮各项结论（✓/×/灰），审核人与工程师双向可见
- **退回自动明确**：勾选「需修改」项自动生成「需要修改的内容」（"请完善：×××、×××"，可编辑）；后端兜底：requirements 为空时由需修改项自动拼装；通过时存在需修改项需二次确认

---

## [v2.2] — 2026-08-05

### 巡检提交资料扩展 + 模板级必传配置（V22）

- **提交资料扩展**：任务详情提交弹窗支持全套资料（一次提交=一个版本=全套留档）：
  - 核心设备文本配置（传文件或粘贴，两者都支持）→ 同步 `DeviceConfigBackup`（`backup_method='巡检上传'`，内容可在线查看）
  - 完整配置备份包（zip，选所属设备）→ 同步 `DeviceConfigBackup`（`backup_type='全部配置'`）
  - 拓扑图（图片/pdf/drawio/xml）→ 同步 `Topology`（按客户，拓扑页自动可见）
  - 资产清单 Excel → **解析导入设备**（按 设备名+客户 upsert，列与设备导入模板一致）
- **新表 `submission_assets`**（迁移 `5f4e3d2c1b0a`）：每轮提交的资料明细（含同步目标 ID 溯源），版本时间线完整可复查；提交资料文件受控下载/文本在线查看
- **模板级必传配置**：`inspection_task_templates` 新增 `required_assets_json`（巡检报告/配置包/文本配置/拓扑图/资产清单 5 项开关，默认仅报告必传），任务模板管理页可视化配置
- **豁免机制**：必传项无法上传时可填原因（`skip_reason` 随版本留档，审核人可见），必传项缺传且无原因 → 服务层拒绝提交
- **Vue 设备页配置备份区块**：设备详情展示配置备份列表（含巡检同步记录）+ 受控下载/在线查看（新 API：列表/内容/下载，替代静态裸暴露）
- 上传同步建档写审计日志

---

## [v2.1] — 2026-08-04

### 巡检/工单审核闭环版本化（V21）

- **提交版本化审核闭环**：新增通用表 `submission_versions`（迁移 `9a8b7c6d5e4f`），
  巡检记录/工单每次「上传报告+提交审核」自动追加一个版本（报告文件 + 提交快照 + 该轮审核结果/意见），
  全部版本永久保留——**单条记录即可复查全部提交历史与每轮审核情况**；退回后工程师看到退回原因
  修改重传（版本递增），直到审核通过
- **巡检任务新增「待审核」状态**：工程师从任务详情上传现场报告（Word/PDF/Excel/图片）→ 自动生成/
  复用巡检记录（任务↔记录 1:1）并提交审核 → 任务「执行中→待审核」；审核通过 → 任务自动「已完成」
  （写 actual_end），退回 → 任务回「执行中」可再传；任务状态机校验（无记录的手工任务仍可直接完成，
  有记录的任务必须存在已通过记录）
- **工单处理报告上传**：提交审核改为「处理报告文件（必传）+ 诊断 + 方案」→ 建提交版本；
  审核意见/审核人写回版本；`_ticket_payload` 补发 audit_* / 报告 / 完整性字段（前端可见退回原因）；
  新增 `ticket:review` 权限码（SSR 审核端点权限同步对齐，巡检 SSR 审核端点统一为 `inspection:review`）
- **按客户 + 自定义时间段查询/导出**：巡检记录（按巡检日期）/工单（按创建时间）列表支持
  `date_from/date_to/customer_id` 过滤；导出 Excel（`/inspections/export`、新增 `/tickets/export`）
  与**报告包 zip**（`/inspections/reports-zip`、`/tickets/reports-zip`：汇总 Excel + 全部版本报告文件，
  按客户/记录组织，防路径穿越）
- **资料完整性核对**：列表/详情计算 `complete + missing_fields`（巡检 7 项 / 工单 6 项），
  列表「完整性」tag 列 + 「仅看不完整」筛选
- **前端**：共享 `VersionTimeline` 组件（版本时间线：提交人/时间/报告下载/审核结果/退回意见）；
  任务看板详情抽屉「上传巡检报告」入口 + 关联巡检记录区块；巡检页新建强制选任务（自动带出客户/
  日期/工程师）；工单页提交审核弹窗（文件上传）；两页筛选区加日期范围 + 导出按钮；
  SSR 双轨同步（任务详情页上传表单、巡检/工单详情页版本历史表、工单提交表单文件上传）
- **修复**：dashboard 待办按 `assigned_to_user_id` 匹配（弃用旧 `inspector_ids` 双轨，纳入待审核态）；
  巡检列表「查看」链接指向未注册路由改为行点击打开抽屉

---

## [v1.1] — 2026-07-19

### 功能补全（v1.1 续）

- **任务模板章节同步**：新建巡检选择任务模板后，报告章节区域按模板 `sections_json` 同步——
  启用状态控制章节显隐、自定义标题实时替换；提交时 `enabled_sections` 随巡检记录保存；
  **Word 报告生成同步消费**：被禁用章节在 docx 中整章移除（旧记录无配置默认全启用，向后兼容）

- **工单录入流程完善**：新建工单可选「我自己接单处置」——录单+派单+接单一体完成（复用状态机与日志），
  默认仍为待派单调度分配
- **故障类别表单内快速新增**：工单表单「故障类型」旁 + 按钮，经新 JSON API（重名校验/sort_order 自动排尾）
  即增即选，无需跳转管理页
- **新建巡检支持任务模板快速创建**：表单顶部新增任务模板选择器，选择后自动用模板关联的设备检查模板
  （标准化 sub_items）加载各设备检查项框架，可再按实际情况修改；新端点
  `/api/customers/<cid>/devices-with-templates` 同时修复了该端点缺失（404）导致的设备加载失败，
  以及新建巡检页缺失 customers 变量导致客户下拉为空的两处潜伏缺陷

- **旧 InspectionTemplate 下线（合同链迁移）**：contracts 新增 `task_template_id`（指向新任务模板），
  迁移 `a8b9c0d1e2f3` 按模板名匹配回填存量合同（无同名则创建最小新任务模板）；
  自动生成器/合同表单/合同任务页全部切换新模板（旧模板只读回退兼容）；旧模板 add/edit/delete
  路由下线，列表页与 API 保留只读（历史任务/巡检表单仍引用）。迁移文件已修复 SQLite 批处理内
  create_index 失败问题（索引移至批处理块外），并经降级重放验证

- **合同自动巡检配置端到端可用**：合同表单补齐「巡检频率/巡检模板/自动生成任务 + 开始/结束日期」字段，
  create/update_contract 持久化（此前表单无字段、service 不持久化，自动生成在新增路径为死逻辑）；
  编辑预填改用 `tojson` 安全序列化（修复标题含引号截断隐患）；checkbox 经 `inspection_config_present`
  标记区分表单提交与局部更新
- **备份包密码保护（可选）**：导出可设密码（PBKDF2 480k → Fernet 整包加密 + magic 头识别），
  导入自动识别加密包并校验密码，错误密码/未提供密码明确拒绝——含密钥的备份包可安全外传/上云

### 修复（v1.1 续）

- **故障记录恢复可编辑/删除**：此前 UI 标注「历史归档仅支持查看」并隐藏了编辑/删除入口（路由实际可用）。
  列表/详情页恢复编辑按钮（fault:edit）与删除按钮（fault:delete，按钮按权限渲染）；
  operator 角色补授 `fault:delete` 权限码；新增「新增故障」入口；提示语更新为引导优先使用工单

### 修复

- **故障记录恢复可编辑/删除**：此前 UI 标注「历史归档仅支持查看」并隐藏了编辑/删除入口（路由实际可用）。
  列表/详情页恢复编辑按钮（fault:edit）与删除按钮（fault:delete，按钮按权限渲染）；
  operator 角色补授 `fault:delete` 权限码；新增「新增故障」入口；提示语更新为引导优先使用工单

### 工程化深化（W5）

- **密钥轮换工具**：`scripts/rotate_secret_key.py`（dry-run 默认、旧密钥自动备份、单事务全量重加密
  + 提交前回读校验、密钥文件原子替换；已端到端验证）
- **密码哈希升级**：旧 pbkdf2 哈希在用户登录成功时透明重哈希为 scrypt（werkzeug 3 默认，零依赖）
- **依赖漏洞清零**：flask 3.1.1→3.1.3、cryptography 44.0.3→48.0.1（pip-audit 项目依赖 5 漏洞→0，
  全量测试回归通过）
- **文档**：新增 `AGENTS.md`（结构/命令/约定/勿动清单）；README 项目结构与技术栈同步新架构
- 累计 **112 用例全绿**

### 数据一致性（W4）

- **状态常量化**：新增 `utils/constants.py` 单一真源（工单/巡检任务/审核/商机/报价/合同/项目/采集任务）；
  sales_service 写入边界校验非法状态直接拒绝；ticket_service / inspection_service 切换引用
- **JSON 边界**：新增 `utils/json_fields.py`（parse_json 失败记日志非静默）；修复模板匹配 API
  `len(items_json)` 数 JSON 字符数的 bug（改用模型 `total_sub_items`）；裸 dict 返回改 jsonify
- **事务完整性**：备件新增两次 commit 改 service 单事务 + 图片失败不阻塞；设备导入逐行 commit
  改整批事务（顺带补齐「是否维修/是否在用」两列长期被忽略的导入缺陷）；工单删除走 form_commit
  + 审计日志；`User.check_password` 明文升级不再隐式 commit（登录流程显式提交）
- **怪异行为清理**：故障类型页不再按 cwd 相对路径双态返回（固定渲染页面）
- **双轨收敛**：新增 `scripts/faults_to_tickets.py` 迁移脚本（dry-run 默认、幂等、fault.ticket_id 桥接）；
  模型层标注旧 InspectionTemplate 仍被合同自动生成链依赖（下线前需先迁 contract.inspection_template_id）
- 新增 W4 测试 10 用例；累计 **111 用例全绿**

### 结构重构（W3）

- **巨型文件拆包**（端点名全部不变，模板零改动）：
  - `ops.py`（1476 行）→ `blueprints/ops/` 8 模块（巡检/任务重定向/模板/故障/工单/知识库/巡检员/报告）
  - `asset.py`（982 行）→ `blueprints/asset/` 4 模块（设备/字典/固件/配置备份）
  - `app.py`（1245 行）→ 325 行，24 个视图迁入 `views/` 包（dashboard/auth/admin_users/system）
  - `models.py`（1108 行）→ `models/` 包 11 模块（`base.py` 持 db 单例，`__init__` 全量 re-export）
- **重复代码消除**：
  - `register_dict_crud` 工厂替代设备类型/品牌/网络类型/自定义字段四份同构 CRUD（-200 行）
  - 四份手写 Excel 导出统一走 `utils.excel_export`；修 `fault_export` 引用不存在的 `Fault.customer`（必 500）
  - `utils.decorators.form_commit` 封装表单写操作，sales/spare 26 个路由收敛
- **依赖清理**：消除 asset→app 循环导入（删 app.py 死封装）；模块级 `os.makedirs` 导入副作用移入 `create_app._ensure_runtime_dirs`
- 新增测试 16 用例；累计 **101 用例全绿**

### 性能优化（W1）

- **N+1 消除**：固件版本库按品牌+型号挂设备（单条 OR 查询替代逐组查询）；机柜列表/详情（selectinload+joinedload）；部门树（head/members 预加载）；报告中心三类记录 customer_rel 预加载
- **报告中心**：首次进入默认近 12 个月窗口（页面有提示，可清空查看全部）；文件反查索引只取有报告文件的记录（原三表全量扫描）
- **首页**：客户名映射按需 IN 加载（替代全表）；巡检任务匹配下推 SQL（逗号包裹防 id 误匹配，如 12 误中 123）；主管视角部门成员去重查询
- **索引迁移**（f7a8b9c0d1e2）：devices(customer_id)、devices(brand,model)、inspection_tasks(status/assigned_to_user_id/contract_id)、inspections(customer_id/review_status)、tickets(customer_id/assigned_to)
- **修复**：报告中心明细行从不渲染（路由写 `items`、模板读 `items_list`，HEAD 上即存在）
- 新增 W1 回归测试 13 用例（各角色首页、SQL 匹配防误伤、固件分组、机柜 API、报告窗口）

### 安全加固（W0）

- **设备明文密码收敛**：设备 JSON（`/api/devices/<id>`）不再携带明文密码；新增 `POST /api/devices/<id>/reveal-password` 按需查看（新权限码 `device:reveal` + 审计日志含操作人/IP）；Excel 导出密码列按权限收敛并记导出审计；设备列表/详情页改为掩码 + 按需查看
- **报告加固**：删除报告改用新权限码 `report:delete`；删除/下载均做 realpath 防路径穿越 + 扩展名白名单；删除写审计日志
- **读 API 权限补齐**：机柜（device:view）、部门树（department:view）、合同任务（contract_auto:manage）、客户级联（customer:view）
- **CSRF 收敛**：取消 drafts/rack 蓝图级豁免（前端 fetch 经 base.html 自动带 X-CSRFToken，保护不变、攻击面收窄）
- **API 错误格式统一**：API 请求未登录/无权限返回 JSON 401/403（兼容蓝图内 `/xxx/api/...` 路径），不再 302 跳登录页
- **知识库浏览计数**：改原子 UPDATE 自增 + 会话去重，修复并发丢失与 GET 副作用问题
- **日志轮转**：`RotatingFileHandler`（10MB × 10），修复 app.log 无限增长

### 重构与缺陷修复（T0）

- `app.py` 改造为 `create_app()` 应用工厂，路由集中注册（端点名不变，模板 url_for 不受影响）；`wsgi.py` 同步更新
- 修复备件删除校验必抛 `TypeError` 的缺陷（`p.stocks.count()` 对 list 调用），备件删除恢复正常
- 修复设备详情页「密码修改历史」从不渲染（路由传 `histories`、模板用 `history_data` 变量名不一致），历史密码改掩码 + 按需 reveal
- 草稿 `related_id` 统一 int 入库（修复 save/load 类型不匹配隐患）
- 实现 `/api/devices/<id>/password-history` 端点（前端原调用 404），列表不含明文

### 工程化（W2）

- 新增 pytest 测试套件 **72 用例**：工单状态机全转换、备件 FIFO/冲销、巡检审核流、四角色权限矩阵、密码安全、CSRF 策略、报告安全、草稿、冒烟
- 新增 GitHub Actions CI（ruff + pytest + pip-audit 告警）、`requirements-dev.txt`、`pyproject.toml`
- ruff 首次全量清理（104 处未用导入/未用变量）

---

## [v1.0] — 2026-06-15

### 首个 GitHub 版本

- Flask 应用主体：14 个业务模块，45 张数据表
- 客户管理 / 设备资产管理 / 巡检任务 / 故障工单 / 知识库 / 备件 / 销售 / 机柜 / AI 集成
- GitHub 一键部署脚本 `scripts/deploy.sh`（Ubuntu 24 + systemd + Gunicorn）
- 在线更新脚本 `scripts/update.sh`（git pull + 零停机重启）
- 备份脚本 `scripts/backup.sh` 和回滚脚本 `scripts/rollback.sh`
- 手动部署迁移脚本 `scripts/migrate-to-github.sh`
- 版本信息文件 `VERSION` 和变更日志 `CHANGELOG.md`

---

## 开发历程（2026-05-22 ~ 2026-06-14）

### 2026-06-14 — V13 用户主数据化
- User 表新增 phone / email / certifications 字段（12 项认证证书）
- 用户自助修改密码 / 管理员强制重置密码
- Inspector 表瘦身，工单派单改为文本输入
- 巡检记录保存时冻结巡检人员快照

### 2026-06-14 — V12 固件版本库
- 新建 device_firmwares 表
- 按品牌+型号分组展示，同型号设备版本对比

### 2026-06-14 — V11 巡检流程标准化
- 设备检查模板富字段编辑（11 种字段类型）
- 任务模板自动匹配 + 章节配置 + 拖拽排序
- 巡检表单草稿自动保存
- 审核通过自动生成 Word 巡检报告

### 2026-06-13 — V6.1 机柜 + 常用工具
- 机柜管理：机房位置 / 机柜 / 设备上架可视化（U 位画布）
- 常用工具：IP 计算 / 子网 / MAC / 进制 / 时间戳 / Base64 / MTU / 带宽 / 报文分析

### 2026-06-13 — V6 四阶段增强
- 备件库字段扩展（+8 字段）
- 拓扑图 UI 重构（按客户分组）
- 知识库附件（多附件上传 + 预览）
- 报告管理重构（客户→类型双层分组）

### 2026-06-12 — P0 阶段
- 工作台合并 Fault + Ticket 统计卡片
- Fault 降级为只读
- 删除重复 AI 配置路由

### 2026-05-22 — 项目创建
- 客户 / 设备 / 巡检 / 故障 / 用户 基础 CRUD
- AES 密码加密 / Excel 导入导出 / Word 报告生成
- Flask-Login 认证 / Bootstrap 5 UI
