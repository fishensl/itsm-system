# G3 隔离恢复演练记录（2026-08-21）

## 范围与隔离边界

- 来源：生产配对备份 `itsm_pg_20260821_053645.dump` 与 `itsm_meta_20260821_053645.tar.gz`。
- 隔离数据库：`itsm_g3_20260821_053645`，演练结束后删除。
- 隔离文件目录：`/tmp/itsm-g3-20260821-*`，演练结束后删除。
- 未停止、未写入生产数据库，未替换生产密钥或业务文件。

## 备份校验

| 项目 | 结果 |
|---|---|
| `pg_restore --list` | 通过 |
| meta `tar -tzf` | 通过 |
| dump SHA256 | `5c52426718515e6d26872a2751d75a2b00f322b15507cc6297ef9fe482d02176` |
| meta SHA256 | `9db3a18bf24a16c2c9c5145649b62d4318b5c81874428b6d2b6fef8812a41b19` |
| 归档文件/恢复文件 | 103 / 103 |
| 恢复密钥权限 | `600` |

## 数据库恢复

首次使用服务器默认 `template0` 时发现其编码为 `SQL_ASCII`，与生产数据库 `UTF8/C/C` 不同，中文字段在恢复时按字节触发长度约束。该隔离库和目录已由清理 trap 删除；随后显式使用 `UTF8`、`LC_COLLATE=C`、`LC_CTYPE=C` 重建隔离库并完整恢复成功。

| 验收项 | 结果 |
|---|---|
| 检查表数 | 64 |
| 生产/恢复表行数差异 | 0 |
| 用户 | 7 |
| 客户 | 37 |
| 设备 | 46 |
| 巡检任务 | 185 |
| 工单 | 1 |
| 通知 | 11 |
| 审计日志 | 32 |
| Alembic 版本 | `b0c1d2e3f4a5` |

## 密文、登录与就绪验收

| 类型 | 密文数 | 成功 | 失败 | 说明 |
|---|---:|---:|---:|---|
| 用户登录 MFA | 1 | 1 | 0 | 使用恢复出的配对密钥解密 |
| 设备密码 | 0 | 0 | 0 | 生产当前无密文样本 |
| 设备扩展凭据/密码历史 | 0 | 0 | 0 | 生产当前无密文样本 |
| AI Key | 0 | 0 | 0 | 生产当前无密文样本 |
| 通知渠道凭据 | 0 | 0 | 0 | 三个外部渠道均未配置 |
| 导出文件密码 | 0 | 0 | 0 | 生产当前无密文样本 |

- 在隔离库创建一次性探针用户，`POST /api/auth/login` 返回 HTTP 200、`code=0`；随后 `/api/auth/me` 返回 HTTP 200、`code=0`。
- `/readyz` 返回 HTTP 200，database、frontend、master_key、migration 均为 `ok/available`。
- 演练完成后确认隔离数据库不存在、隔离目录不存在，清理结果为 `ok`。

## 结论

G3 通过。当前备份可以在匹配生产编码和 locale 的 PostgreSQL 隔离库中恢复，配对密钥可解密现有 MFA 密文，应用登录及就绪检查正常。恢复手册必须显式记录数据库编码参数，不能直接继承本机 `template0` 默认值。
