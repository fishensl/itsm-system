# 生产客户数据范围补录与启用记录（2026-08-21）

> 状态：只读盘点和候选关系生成已完成；尚未写入生产，`ITSM_CUSTOMER_SCOPE_ENFORCE` 仍为 `false`。

## 1. 当前生产事实

- 生产代码：`48db119`；`itsm.service`、`itsm-backup.timer` 均为 `active`。
- 用户 7 人，其中活跃 5 人：管理员 1 人，活跃非管理员 4 人。
- 客户 37 个，设备 46 台；46 台设备均属于“鄱阳湖水文”。
- `customer_engineers` 当前为 0 条，所有用户的 `region_ids` 也为空。
- 3 名活跃运维科人员和 1 名活跃技术部人员的 `scope` 均为 `department`。
- 组织树为“技术部 → 运维科/工程科”，运维科负责人已配置为卢冠星（user_id=2）；
  技术部未配置负责人。

生产当前版本中，`department` 只按相同 `department_id` 取并集，未包含下级部门；这与组织树
语义不一致。启用前先修正为“本部门及全部下级部门”：技术部可继承运维科/工程科，运维科
不反向继承技术部，也不访问兄弟部门工程科。管理员始终拥有全量范围。

## 2. 历史数据候选关系

候选关系只采用生产库中的结构化人员字段：巡检任务 `assigned_to_user_id` 和巡检记录
`inspector_user_id`。工单仅有 1 条文本负责人“管理员”，不作为工程师关系依据。

### 2.1 活跃人员候选客户

以下关系尚未写入生产；同一客户可以有多名历史活跃负责人。

| 活跃人员 | 部门 | 历史证据支持的客户 |
|---|---|---|
| 卢冠星（user_id=2） | 运维科 | 信江饶河水文（景德镇大队）、外洲大队、景德镇城防、景德镇城防排涝站、景德镇市昌南新区、水科院共青城、水科院德安、永丰县水利局、赣江上游水文、赣江中游水文、进贤大队、鄱阳湖水文、铅山县水利局、鹰潭市水利局 |
| 张鑫越（user_id=6） | 运维科 | 吉安市水利局、吉州区水利局、昌江区水利局、赣江中游水文、遂川县水利局、鄱阳县水利局、铜鼓县水利局、鹰潭市余江区水利局、鹰潭市水利局 |
| 邱斌（user_id=9） | 运维科 | 信江新区社会事务局、景德镇城防、景德镇城防排涝站、景德镇市水利局、鄱阳县水利局、鄱阳湖水文、高新区社会事务局 |
| 何志康（user_id=10） | 技术部 | 信江饶河水文（上饶） |

按上述候选关系写入但不补接管人时：

- 运维科 3 名活跃用户因 `department` 范围共同可见 24 个客户；
- 技术部 1 名活跃用户在层级语义修复后可见运维科 24 个客户及其直接关联的 1 个客户；
- 12 个客户仍没有任何活跃负责人；
- “鄱阳湖水文”属于运维科候选范围；层级语义修复后，其 46 台设备对运维科和父级技术部均可见。

### 2.2 仅有停用人员证据的 12 个客户

| 原历史人员 | 待确认接管客户 |
|---|---|
| 黄维辉（user_id=7，已停用） | 万安县水利局、安福县水利局、新干县水利局、水科院西区、永新县水利局、江西省水利科学院、泰和县水利局、鄱阳湖大队、鄱阳湖大队都昌站 |
| 贾炯（user_id=8，已停用） | 万家埠水文站、庐山大队、李家渡水文站 |

停用人员不会进入部门范围并集。即使把关系继续保留在停用账号上，也不能让活跃人员看到这些客户。

## 3. 启用前必须确认的业务规则

1. 运维科采用“部门共享客户池”还是“每人仅看自己客户”。建议第一阶段保持
   `department`，避免突然改变现有协作方式；若选择个人独占，需要把对应用户改为
   `self`，并补充管理入口和回归测试。
2. 为上述 12 个客户指定至少一名活跃接管人。
3. 确认技术部何志康是否应按父部门范围查看运维科全部客户；建议按组织树向下继承，
   不要求为父部门人员重复写入子部门的全部客户关系。
4. 确认共享客户是否保留多个直接负责人；建议保留全部有活跃历史证据的人员，便于派单和审计。

建议的低扰动首批规则是：运维科继续使用部门共享客户池；保留全部活跃历史关系；原黄维辉、
贾炯名下且没有活跃接管证据的 12 个客户先转给运维科负责人卢冠星。这样 37 个客户都有活跃
责任部门，运维科不会因开关启用而缺失客户。修复父子部门范围后，技术部会继承运维科客户，
并额外保留何志康直接负责的“信江饶河水文（上饶）”。

## 4. 确认后的生产执行步骤

1. 生成并校验一次新的数据库/meta 配对备份。
2. 保存 `customer_engineers` 变更前快照；按已确认映射事务写入并记录操作审计。
3. 保持强制开关关闭，核对每名活跃用户配置客户数、部门有效客户数、孤立客户数和设备数。
4. 先在独立进程临时开启强制配置做只读 API 探针，再选择一名工程师进行真实页面试用。
5. 试用通过后设置 `ITSM_CUSTOMER_SCOPE_ENFORCE=1` 并重启服务。
6. 验证客户列表/树/详情、设备列表/详情、全局搜索、下拉字典和导出；越权直接 ID 应返回 404。
7. 任一用户缺少应有数据时立即关闭开关并重启；关系数据保留用于修正，不需要回滚数据库。

## 5. 当前停止条件

在第 3 节三项业务归属未确认前，不写入候选关系、不启用范围强制开关。历史任务只能作为
候选证据，不能替代当前客户责任人的业务授权。
